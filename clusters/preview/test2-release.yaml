apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sonarqube
  namespace: sonarqube
spec:
  chart:
    spec:
      chart: ./charts/sonarqube/
      sourceRef:
        kind: HelmRepository
        name: sonarqube
        namespace: flux-system
  interval: 1m0s
